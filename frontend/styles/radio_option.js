import styled from 'styled-components';

export default styled.div`
  > input {
    margin-right: 6px;
  }

  > label {
    font-weight: lighter;
    font-size: 14px;
    text-transform:capitalize;
  }
`;
