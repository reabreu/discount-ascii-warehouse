import styled from 'styled-components';

export default styled.div`
  padding-bottom: 40px;
  position: relative;
  margin-top: 20px;

  .loader-wrapper {
    position: absolute;
    bottom: 0;
    transform: translateX(-50%) scale(0.5);
    left: 50%;
  }
`;
