import React from 'react';
import SpinningLoader from '../../styles/spinning_loader';

export default function () {
  return (
    <SpinningLoader className="loader">Loading...</SpinningLoader>
  );
}
